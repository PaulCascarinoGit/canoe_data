# FastAPI + Keycloak + Shiny Integration

## Requirements
- Docker
- Docker Compose

## Setup
1. Add your `.env` with required variables.
2. Run `docker-compose up --build`.

## Test the API
Use `pytest` in the `api` directory.

## Access Shiny App
Visit `http://localhost:3838`.
